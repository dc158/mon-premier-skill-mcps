services:
  - type: web
    name: webdesign-co-webhook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --webhook
    envVars:
      - key: WEBHOOK_SECRET
        generateValue: true
      - key: PORT
        value: 10000
    autoDeploy: true
